# Project Sounding Line — detailed closure chat record

Date: 2026-07-31

> This is the most detailed repository-safe record of the Codex conversation
> and work state available from this task. It deliberately excludes private
> tokens, machine credentials, and browser screenshots/traces; those items are
> archived outside Git with hashes.

## User’s governing instruction

The user directed Codex to complete **Project Sounding Line** absolutely, not
to return a partial-completion report, and never to claim completion while any
final acceptance item was false. The request named `Kgray44/treasurehuntSoT`,
the known main baseline `424ecc3b7a15ad53fc591287968720829c27f6ae`, merged PR
`#6`, and task head `acd91cedc4d4641d72bde859314dedf1d58e427c`.

The required outcome is: independently selectable nonempty browser families;
focused clean receipts; one complete sealed release-candidate plan with a
unique passing/clean receipt for every required suite; a `RELEASE_GO` only
from Sounding Line’s finalizer; P34 not selected or executed; evidence archive;
accurate completion documents; protected PR merge; exact `origin/main` parity;
and remaining limitations `NONE` only when all of those facts are true.

The user prohibited force/reset/clean/stash/broad staging, branch-protection
bypass, raw Playwright as authority, P34 execution, test weakening, skips to
hide failures, broad browser-family removal, unrecorded evidence deletion, and
duplicate unchanged complete candidates. Corrections must follow exact failed
receipt -> focused cause -> narrow fix -> regression proof -> corrected full
candidate.

## Repository and authority facts established

- Closure worktree: `C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\treasurehuntSoT-sounding-line-absolute-final-closure`
- Closure branch: `codex/project-sounding-line-absolute-final-closure`
- Sounding Line remains the sole testing authority and its finalizer remains
  the sole release-decision authority.
- Main protection requires `Sounding Line / Mainline Decision`; focused runs
  are evidence-only.
- Active registry: schema `2.0.0`, 1,267 cases, 39 owned active families.
- Release candidate requires 42 suites, including 16 browser suites and 325
  registered browser cases.
- P34 ledger: 316 historical identities, classified 314
  `CURRENT_CONTRACT_MIGRATED` and 2 `REPLACED_CANONICAL`; P34 is not a current
  test selection.
- Required browser suites: `browser.access-sentinel`, `browser.auth`,
  `browser.invitations`, `browser.player-library`, `browser.player-journal`,
  `browser.passport`, `browser.artifacts`, `browser.captain`, `browser.studio`,
  `browser.community`, `browser.private-operations`, `browser.navigation`,
  `browser.accessibility`, `browser.responsive`,
  `browser.animation-lifecycle`, and `browser.cross-project`.

## Repairs and validation conversation

1. Browser selection was made exact from the generated registry: the planner,
   Playwright adapter, and isolated runtime reconcile registration and discovery
   rather than treating a broad raw Playwright call as authority.
2. Focused browser evidence was retained in the external archive. It included
   all required families; later source-level runtime/workflow corrections are
   being proven by the complete current candidate rather than relabeling old
   receipts as current proof.
3. The first complete candidate (`30632060176`, source `dcf63d...`) produced
   a clean but failed `static.core` receipt due to Prettier drift and a failed
   Sounding Line runtime worker caused by absent Chromium. The formatting and
   Captain focus state were corrected; `unit.sounding-line` was declared to
   require the sealed Chromium resource.
4. The second candidate (`30633408371`, source `cbfce760...`) exposed the
   exact hosted bootstrap error caused by passing `@engines` as an install
   target. Its worker correctly failed closed for the missing receipt. The run
   was preserved and cancelled because it could not issue `RELEASE_GO`.
5. The engine invocation was corrected to explicit `npx playwright install
   chromium` or `chromium webkit`, and verified through the Sounding Line
   authority-cutover test.
6. The next candidate (`30634019830`, source `e1942272...`) exposed a stale
   PowerShell `$LASTEXITCODE` check for suites which had not run a browser
   installer. Its plan, logs, and available receipt were archived; it was
   cancelled once its common bootstrap failure made a decision impossible.
7. The guard was narrowly changed to check `$LASTEXITCODE` only when
   `$browserEngineInstallInvoked` is true. The source test asserts that exact
   contract. The focused authority test passed 8/8. This correction is commit
   `988e1b2ded70cce1bfa0f16e1524aff8903a27ab`.

## Current authoritative proof

The active complete hosted release candidate is:

- Workflow run: `30634432339`
- Tested source: `988e1b2ded70cce1bfa0f16e1524aff8903a27ab`
- Gate: `release-candidate`
- Sealed plan: 42 nodes
- Policy digest: `0cdcdd66de9352fe804a65a462235d439ba4c6e5ea80c03f451c6e974646b00e`
- Inventory digest: `c6e3c31a4047a1c07680280e199e1218948f42f4462865a7a8e86e5e6d3d4ece`
- Plan digest: `0ce3bf30f396a5d964d961f9787ed8e39562e80e1456b4126b9b8d88d9d530d0`

At the time of this chat amendment, 21 governed jobs have completed
successfully, zero have failed, and `browser.invitations` is the single active
exclusive worker. Completed browser proof in this run includes access sentinel,
accessibility, animation lifecycle, artifacts, auth, captain, community, and
cross-project. It is not yet a `RELEASE_GO`; the remaining worker receipts and
finalizer are still required.

The executable/policy identity was computed from 930 tracked source, test,
script, workflow, Prisma, package, and testing-policy inputs. Its digest is
`718febe90c8c9752b8423f0d4354af575a72af8535bdd1139359c10a8f8f02ec`.
Documentation-only edits must reproduce this identity before protected merge.

## Evidence archival and cleanup conversation

The prior program-completion worktree contained 47 untracked artifacts. All
were copied and SHA-256 matched at:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-prior-program-completion-worktree`

The archive manifest records 38 canonical local receipt duplicates, 3
machine-specific logs, 1 temporary preflight artifact, and 5 private or
machine-specific diagnostics (Playwright image/trace/video/report). The
environment rejected the attempted exact targeted removal. The originals were
therefore left in place rather than bypassing the deletion safety control.

## User follow-ups in this chat

The user first asked how much work remained. Codex replied accurately that the
candidate was 20/42, all completed jobs were successful, and the remaining
work after finalizer included receipt reconciliation, closure documentation,
protected PR merge, and final parity.

The user then requested a Markdown record of the chat under `Codex_Chats`,
committed and pushed as the only document in that commit, while testing
continued. Codex created and pushed commit `e8034cd3c` containing only this
file. The user then noted that the record was insufficiently detailed and
requested this replacement, including current progress, followed by continued
testing. This amendment is the requested detailed replacement and remains
documentation-only.

## Completion boundary

No completion statement is authorized by this record. Codex must still obtain
and independently reconcile all 42 current receipts and the finalizer decision,
archive final evidence, update governed closure documentation and ledger,
validate the unchanged executable identity, merge through required protection,
verify `origin/main`, and only then report the user’s mandated completion
wording with `NONE` remaining limitations.

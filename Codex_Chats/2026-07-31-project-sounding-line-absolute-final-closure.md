# Project Sounding Line — complete Codex chat and closure record

**Chat date:** 2026-07-31
**Repository:** `Kgray44/treasurehuntSoT`
**Closure branch:** `codex/project-sounding-line-absolute-final-closure`
**Scope:** Project Sounding Line absolute final closure

> This is the detailed repository-safe record of this Codex conversation and
> the engineering work performed under it. It preserves user instructions,
> major assistant decisions, commits, receipts, failures, repairs, archived
> evidence, and the current live state. It deliberately omits credentials,
> environment secrets, private browser screenshots, browser profiles, trace
> payloads, databases, and machine dumps. Those are not appropriate Git
> content and, where retained, are held in the task-owned external evidence
> archive with hashes.
>
> Timestamps are the session's observed order unless a hosted receipt supplies
> an authoritative time. A running workflow is described as running, never as
> passing, until its governed receipt and finalizer are independently checked.

---

## 1. User messages and governing intent

### 1.1 Original program-completion instruction

The user first supplied the Sounding Line final-program-completion governing
text. Its mission was to preserve the already merged authority cutover and
finish the actual program: migrate tests into owned families, semantically
retire P34, complete metadata and policy governance, enforce performance
budgets, generate canonical documentation, establish Stage 10 records, and
merge through protected mainline integration.

The instruction established these non-negotiable authority facts:

- Sounding Line owns `npm test` and `npm run validate`.
- Raw runners and focused execution are non-authoritative.
- Workers consume sealed, nonserial plans and emit node-scoped receipts.
- The finalizer alone makes release decisions.
- `main` requires `Sounding Line / Mainline Decision`.
- `release.full` remains removed and `scripts/test-all.ps1` remains
  compatibility-only.
- P34 is archived, unselectable, and must not be restored or executed.

The user required autonomous continuation through ordinary engineering
failures; no incomplete final response was allowed unless the remaining work
required an owner-only permission, credential, or irreversible product
decision after every safe independent action had been exhausted. The user also
forbade destructive Git operations (`reset --hard`, `clean`, `stash --all`,
force push, broad staging), protection bypass, test weakening, arbitrary
timeouts/retries, skips that conceal failures, and unrecorded deletion of
evidence.

The initial program contract required semantic disposition and traceability
for all 316 historical P34 identities, one active owned family for every
current executable test, exact registry/discovery reconciliation, governed
browser ownership and cleanup, measured/enforced budgets, documentation
generated from machine facts, and protected mainline integration. Its historic
success wording was not used because the program later moved into the more
specific absolute-final-closure phase.

### 1.2 Absolute-final-closure instruction

The user then issued **PROJECT SOUNDING LINE — ABSOLUTE FINAL CLOSURE**. This
superseded any interpretation that a checkpoint, one green sentinel, one
browser family, a green wrapper, a local document, or an exception describing
the browser matrix as pending could count as completion.

The user identified the merged baseline as:

- known `main`: `424ecc3b7a15ad53fc591287968720829c27f6ae`;
- merged PR: `#6`;
- known task head: `acd91cedc4d4641d72bde859314dedf1d58e427c`.

The instruction required fetching `origin/main` first and preserving any newer
state. It stated that the core authority architecture was already merged:
Sounding Line as test/release authority, protection, the mainline decision,
registry schema `2.0.0`, owned unit/component/browser families, 39 active
families, P34 semantic retirement, budgets, Stage 10 records, and plan/worker/
evidence/finalizer infrastructure.

The remaining work was explicitly limited to proving the complete governed
browser release candidate, repairing browser-family defects through focused
evidence, obtaining a real finalizer `RELEASE_GO`, correcting stale completion
documents, retaining evidence, resolving task-owned local evidence safely,
merging closure records through protection, and proving a clean final state.

The user required an inventory for every current browser family from the
machine-readable gate and suite definitions. That inventory must reconcile
suite ID, owner, tier/risk, gate, adapter, test IDs/counts, Playwright
discovery, browser/device/viewport/motion/network policy, fixtures, databases,
servers/ports, storage/trace roots, budgets/dependencies, and cleanup. Empty,
unknown, duplicate-primary, unregistered, P34, or retired-architecture
ownership is fail-closed.

The closure required a CSV ledger at:

`Development_Docs/Programs/Sounding_Line/Project_Sounding_Line_Final_Browser_Closure_Ledger.csv`

with one row per required browser suite and fields for counts, profiles,
isolation, focused and release-candidate evidence, cleanup, artifact/digest,
and remaining limitation. Success fields must only be filled after evidence
exists; final limitations must be empty in every ledger row.

The user required focused validation in dependency-aware batches before an
eligible full re-run, using Sounding Line's sealed selected-suite mechanism as
authority rather than raw Playwright. Every focused receipt had to show source,
policy/inventory/plan identity, reconciliation counts and timings, exit 0,
`PASSED`, `CLEAN`, no unexplained skips/leaks/P34, and no arbitrary retry.

The required focused families are determined by the current release gate, not
by remembered names. At this closure point they are:

1. `browser.access-sentinel`
2. `browser.auth`
3. `browser.invitations`
4. `browser.player-library`
5. `browser.player-journal`
6. `browser.passport`
7. `browser.artifacts`
8. `browser.captain`
9. `browser.studio`
10. `browser.community`
11. `browser.private-operations`
12. `browser.navigation`
13. `browser.accessibility`
14. `browser.responsive`
15. `browser.animation-lifecycle`
16. `browser.cross-project`

The user prohibited circular testing. Every full release-candidate retry needs
the prior root failure, a concrete corrective commit, focused proof of that
correction, and no known unresolved family failure. Only one complete run may
be active at once.

The full release candidate must use the existing planner, sealed plan,
governed workers, node receipts, and finalizer. It must contain the policy,
unit/component/database/browser/P34/build and any currently mandatory
privacy/security suites. The finalizer must reject missing, duplicate, unknown,
failed, mismatched, uncleared, skipped, or P34-including evidence. Only a
complete reconciliation may emit `RELEASE_GO`.

After a passing candidate, documentation must accurately identify the source,
digests, run IDs, receipt totals, browser reconciliation, P34 classification,
evidence, protected merge, cleanup and **`NONE`** remaining limitations. A
documentation-only post-proof change is allowed without another browser matrix
only after executable/policy-input digest equality is recorded and
revalidated. Final integration must happen through a normal protected PR; if a
final merge SHA requires a subsequent receipt amendment, that amendment must
be a docs-only protected PR with unchanged executable identity and a green
mainline decision.

### 1.3 User status question

The user asked, “How much is left to do?” The response correctly distinguished
the completed authority architecture and focused repair work from the still
unproven full release candidate. It did **not** call the project complete.

### 1.4 User chat-archive request

The user then instructed Codex to make an `.md` document containing the entire
chat so far, commit and push **only that document** inside `Codex_Chats`, and
continue tests. A first archival document was committed as `e8034cd3c`, but it
was short and became stale as repairs and run identities changed.

The user explicitly corrected that outcome: “In that document you just
pushed, it is basically empty; I want this ENTIRE CHAT inside it! Replace it
with the most detailed version possible, including your current progress, and
then commit and push it to GitHub again. And then continue with your tests.”

This file is that replacement. The commit that carries it is intentionally
limited to this Markdown file; no completion-document, product, test, or
workflow changes are bundled with this archive amendment.

---

## 2. Current authority and repository state

The active closure worktree is:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\treasurehuntSoT-sounding-line-absolute-final-closure`

The active closure branch is:

`codex/project-sounding-line-absolute-final-closure`

The release authority remains Sounding Line. Branch protection requires the
exact `Sounding Line / Mainline Decision` check. Focused runs are evidence-only
and have not been treated as release decisions. `release.full` remains absent;
`scripts/test-all.ps1` remains compatibility-only. The finalizer has not been
replaced or bypassed.

The active registry at the latest tested source contains **1,270** governed
cases in **39** active owned families. The browser portion comprises **325**
registered governed cases across the 16 listed required suites. The P34 ledger
contains **316** historical identities: **314** `CURRENT_CONTRACT_MIGRATED`
and **2** `REPLACED_CANONICAL`. P34 is not an active selectable suite and must
not execute.

The tracked closure documentation has deliberately been prepared but not yet
committed because it cannot truthfully state final evidence before the current
complete candidate passes and is independently reconciled. The prepared files
are the program receipt/design/inventory/performance report, document index,
README where testing language was stale, and the final browser closure ledger.
They will be finalized only with actual run/receipt/merge facts.

---

## 3. Evidence and repair timeline

### 3.1 Earlier candidate context

Earlier release-candidate attempts proved the governing pipeline and exposed
real bootstrap/policy defects. Their receipts and logs were retained rather
than relabeled as a final proof. Examples include static formatting drift,
absent browser bootstrap in the hosted worker, an invalid browser-install
invocation, and a stale PowerShell `$LASTEXITCODE` interpretation for nodes
that never invoked an installer. Each was repaired narrowly through the
authority system rather than hidden by retries or broad exclusions.

The browser-proof sequence in this exact closure branch begins with the
current focused, source-specific failures and repairs below. The purpose of
preserving earlier evidence is diagnostic traceability; it is not evidence that
the final current tree is green.

### 3.2 Studio draft-save race repair

**Failure class:** Studio product/fixture race during publish.

**Corrective commit:**

`ff6da359f4b31978e999a2834a209d091443ada9`
`fix(studio): await in-flight draft saves before publish`

The repair caused `TaleEditor.save()` to retain its in-flight promise. A
duplicate save now awaits that promise instead of returning a misleading false
result while publish is depending on the save operation. A focused local
component proof passed 7/7 and was archived at:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-focused-component-studio-save-publish-race`

The hosted focused Studio run `30682388922` then passed **8/8**, exit code 0,
with cleanup `CLEAN`. Its evidence archive is:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-focused-studio-30682388922-passed`

### 3.3 Animation-lifecycle failure and Rive fallback repair

**Failing full candidate:** `30682561386`, source `ff6da359...`
**Exact failing family:** `browser.animation-lifecycle`
**Failure class:** animation lifecycle / React render loop.

The real first causal fault was an unstable callback path in the malformed Rive
fallback. It issued a state update every render, producing React's maximum
update-depth failure and preventing the expected PageFlip lifecycle counts.
The run was preserved and safely cancelled after the exact failure became
known; it was not used as a release decision.

**Corrective commit:**

`d9598ac7675e46750f2fbdd855e6bb7559f03d38`
`fix(animation): stabilize Rive fallback status`

`RiveRuntime.tsx` now holds the status callback in a ref and runs the fallback
effect only for the asset key. The associated Rive Runtime test exercises a
stateful inline callback regression. Local focused Rive tests passed 8/8;
TypeScript and policy validation passed.

The focused hosted animation proof was run `30684719116`, source `d9598...`:

- 57/57 cases passed;
- exit code 0;
- cleanup `CLEAN`;
- duration 1,728,811 ms;
- policy digest `0cdcdd66de9352fe804a65a462235d439ba4c6e5ea80c03f451c6e974646b00e`;
- inventory digest `1380f...`;
- plan digest `8b767...`;
- receipt SHA-256
  `4ED662486C7BFD48E76EDEFF3756AE9B8F53352097E15F91409197D6FD0CF918`.

Archive:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-focused-animation-lifecycle-30684719116-passed`

### 3.4 Studio publication/presentation handoff failure

**Failing full candidate:** `30685695979`, source `d9598...`
**Exact failing family:** `browser.studio`
**Failing test:** `creator authors, aligns, publishes, plays, and reviews a media-rich tale`.

The governed Studio receipt reported four Chromium cases passing and the target
publication case failing with “Publishing failed” after it had shown
“Publishing...”. The receipt itself was a cleanly produced failure (exit 1,
cleanup `CLEAN`); a green workflow wrapper was not allowed to overrule it.
The archive is:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-release-candidate-30685695979-failed-studio`

Diagnostic test instrumentation was committed in
`55914427fb5d50e7a4dec41eb43534d210c5d8ef` (`test(studio): surface publish
API failures`). Its first form accidentally referenced `tale!.id` outside the
test-local scope; that was a genuine test defect, repaired by:

`fb0eeeae8c98ccc2dc8e8aab8ebb645e7d7e51fd`
`fix(test): bind Studio publish response to local tale`

Focused Studio run `30690772599` then identified the diagnostic scope error;
it was retained as diagnostic evidence, not counted as a product pass:

`...\SoundingLine_Evidence_Archive\20260731-focused-studio-30690772599-diagnostic`

Focused Studio run `30691024970` on `fb0eee...` passed 8/8 with exit 0 and
`CLEAN`. It demonstrated that the source could pass, but it did not erase the
intermittent presentation handoff explanation. Its evidence archive:

`...\SoundingLine_Evidence_Archive\20260731-focused-studio-30691024970-passed-diagnostic`

It recorded policy `0cdcdd...`, inventory `1380f...`, plan `bd3fc5...`, and
receipt SHA-256
`73014C43F117107F4C71318DA18F60E4CA046B6EBB3375637F89AA2C678F50D7`.

### 3.5 Final Studio presentation-handoff repair

**Corrective commit:**

`b877ca5e59478e9c3a82526885fe5be509c7b9d4`
`fix(studio): retain completed publishes after presentation errors`

The causal distinction is now explicit in `TaleEditor.publish()`: an immutable
publish operation may already have resolved successfully before the optional
presentation director throws. The previous catch treated either type of error
as a failed publish and told the user “Publishing failed” despite a published
version existing. The repaired catch awaits/inspects the retained operation
promise. It emits the product error only when no completed version label exists;
otherwise it preserves the completed version and reports it as confirmed.

The associated component regression uses a hoisted `animationDirector` mock
and proves that a completed publish remains confirmed when the presentation
director interrupts afterward. Focused raw component diagnostics passed 8/8,
the TypeScript compilation passed, and Sounding Line policy validation passed.

The registry was regenerated because the new regression changes the active
governed test set. It now reconciles to **1,270** total cases (not the stale
1,267/1,269 historical values). Its textual ordering churn was generator
output; the semantic change is the registered test addition.

The required hosted focused proof for this exact repair was run `30691324716`
on `b877ca5e...`:

- suite `browser.studio`;
- 8 registered/discovered/executed/passed cases;
- 0 failed, 0 skipped;
- exit code 0;
- cleanup `CLEAN`;
- duration 210,961 ms;
- policy digest `0cdcdd66de9352fe804a65a462235d439ba4c6e5ea80c03f451c6e974646b00e`;
- inventory digest
  `33ec75d10d31c8137cadeb19a4d29b44162bae7db77ad340350e3e8744446850`;
- plan digest
  `a108dbe7aee21b22f04f530e42654806dba978abb51fb1ff67558edc5b4f0aa4`;
- receipt SHA-256
  `6E1B9F4F2A28E8CB1DD37CEE7F86B4667270DD83CE0084FA47F8B906CE4DE476`.

Archive:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-focused-studio-30691324716-passed-presentation-handoff`

This is the focused prerequisite for the current complete release candidate.

---

## 4. Current complete release-candidate execution

The single active corrected complete governed candidate is:

- **Hosted run:** `30691520484`
- **Workflow:** `Sounding Line authoritative`
- **Dispatch gate:** `release-candidate`
- **Tested source:**
  `b877ca5e59478e9c3a82526885fe5be509c7b9d4`

It uses the normal planner, sealed plan, governed workers, node receipts, and
finalizer. It is not a raw Playwright invocation and it is not a new parallel
authority path. At the time this archive amendment was prepared, sealed-plan
and wave-zero work had passed, for **14 successful governed jobs**, zero failed
jobs, and the sole active job was:

`Sounding Line / Exclusive wave 0 (browser.accessibility) / Sounding Line / Governed worker`

The precise status is deliberately recorded as **in progress**. No finalizer
decision, full browser total, receipt total, evidence digest, or release result
is entered as a fact here until the hosted artifacts are downloaded and the
receipts are reconciled independently.

The next mandated actions after a successful workflow conclusion are:

1. Download every worker artifact and the finalization artifact into a dated
   task-owned external archive.
2. Parse/reconcile the sealed plan and every receipt: one receipt per required
   suite, no duplicates/unknown/missing nodes, source/policy/inventory/plan
   identity match, result `PASSED`, exit 0, cleanup `CLEAN`, no unexplained
   skips, and no P34 execution.
3. Reconcile all browser receipts to 325 registered/discovered/executed cases
   and the 16 required browser families.
4. Verify the finalizer's actual decision is `RELEASE_GO`, rather than relying
   on the workflow’s green check.
5. Update the closure ledger, program receipt/design/inventory/performance
   report/index/README only with those real facts.
6. Recompute the executable/policy input digest and prove equality before
   treating documentation changes as docs-only.
7. Validate documentation, policy, registry, and Sounding Line authority
   tests; commit the safe summary documents intentionally.
8. Integrate through a protected PR, await a mainline `RELEASE_GO`, merge
   normally, fetch `origin/main`, and verify exact ancestry/tree parity.
9. If necessary, create one docs-only protected amendment for actual merge
   SHA/main SHA fields, with executable identity unchanged.

No final-completion wording is allowed until those actions are true.

---

## 5. Evidence archival and cleanup handling

The prior program-completion worktree had 47 untracked artifact items. They
were copied with SHA-256 verification into:

`C:\Users\kgray\AppData\Local\ForeverTreasureCompanion\SoundingLine_Evidence_Archive\20260731-prior-program-completion-worktree`

The archive manifest classifies them as 38 safe local receipt duplicates, 3
machine-specific logs, 1 temporary preflight item, and 5 private or
machine-specific diagnostics (including browser trace/video/report material).
Private data has not been committed. A targeted removal attempt was refused by
the local platform; the originals were left intact rather than using a
prohibited broader deletion operation. The final closure receipt must state the
actual resolved condition, not imply a deletion occurred.

The current focused and future full-candidate hosted evidence is kept in the
same external stable archive root. Repository commits contain only safe
summaries, hashes, run IDs, and governed receipt metadata where policy permits.

---

## 6. Required final acceptance boundary

The project is **not complete** at the time of this document replacement.
Remaining substantive checks include the in-progress release candidate,
independent evidence reconciliation, truthful final docs/ledger, protected PR
mainline decision and normal merge, mainline parity, and final resource/
worktree/workflow cleanup verification.

The release cannot be called green merely because early jobs are successful.
Completion remains conditional on every required receipt, every browser suite,
all cleanup states, the unique finalizer `RELEASE_GO`, protected integration,
and factual records on `origin/main`.

When and only when those facts exist, the user requires the final response to
begin exactly:

```text
PROJECT SOUNDING LINE ABSOLUTELY COMPLETE

FULL GOVERNED BROWSER MATRIX PROVEN

RELEASE-CANDIDATE AUTHORITY GREEN

P34 REMAINS FULLY RETIRED

STAGE_10_LEGACY_AUTHORITY_REMOVED

ORIGIN/MAIN FINALIZED AND SYNCHRONIZED
```

Until then, this archive is a detailed progress record, not a completion
receipt and not a claim that the current active candidate has passed.

"use client";

import Link from "next/link";
import { useEffect, useState, type FormEvent } from "react";
import { usePersonalHarbor } from "@/components/homeport/PersonalHarborLayout";

type LoadState<T> = { status: "loading" } | { status: "error"; message: string } | { status: "ready"; value: T };
async function responseBody<T>(response: Response): Promise<T> {
  const body = (await response.json().catch(() => ({}))) as T & { error?: string };
  if (!response.ok) throw new Error(body.error || "This request could not be completed.");
  return body;
}
function useResource<T>(url: string) {
  const [generation, setGeneration] = useState(0);
  const [state, setState] = useState<LoadState<T>>({ status: "loading" });
  useEffect(() => {
    const controller = new AbortController(); setState({ status: "loading" });
    fetch(url, { cache: "no-store", signal: controller.signal }).then(responseBody<T>).then((value) => setState({ status: "ready", value })).catch((cause) => { if (!controller.signal.aborted) setState({ status: "error", message: cause instanceof Error ? cause.message : "This section is unavailable." }); });
    return () => controller.abort();
  }, [generation, url]);
  return { state, reload: () => setGeneration((value) => value + 1), setState };
}
function LoadingState({ label = "Opening your Chronicle Passport" }: { label?: string }) { return <div className="harbor-state harbor-state--loading" role="status"><span aria-hidden="true" />{label}…</div>; }
function ErrorState({ message, retry }: { message: string; retry: () => void }) { return <div className="harbor-state harbor-state--error" role="alert"><h2>This section is unavailable</h2><p>{message}</p><button type="button" className="button" onClick={retry}>Try again</button></div>; }
function MutationMessage({ message, error = false }: { message: string; error?: boolean }) { return message ? <p className={`harbor-mutation ${error ? "harbor-mutation--error" : "harbor-mutation--saved"}`} role={error ? "alert" : "status"} aria-live="polite">{message}</p> : null; }

type PassportOverviewDto = { sections: Array<{ id: string; label: string; href: string; count: number; empty: string }> };
export function PassportHome() {
  const { state, reload } = useResource<PassportOverviewDto>("/api/passport/overview");
  if (state.status === "loading") return <LoadingState />; if (state.status === "error") return <ErrorState message={state.message} retry={reload} />;
  return <div className="harbor-stack"><section className="passport-intro"><p className="personal-harbor__eyebrow">Your record, in one place</p><h2>The Chronicle Passport</h2><p>Return to the Voyages you joined, the private Memories you kept, artifacts authoritatively granted to you, and eligible Community items you saved. No engineering simulators or test controls live here.</p></section><div className="passport-card-grid">{state.value.sections.map((section) => <Link href={section.href} key={section.id} className="passport-card"><span>{section.count}</span><h2>{section.label}</h2><p>{section.count ? "Open this part of your record." : section.empty}</p><strong>Open <span aria-hidden="true">→</span></strong></Link>)}</div><section className="harbor-callout"><strong>Profile and Passport are related, but not the same.</strong><p>Your public Profile is the identity other people may see. Your Chronicle Passport is the private, record-led home for your own Voyage history.</p><Link href="/account/profile">Edit public Profile</Link></section></div>;
}

type HistoryItem = { id: string; chronicle: { title: string; cover: string | null; versionChecksum: string }; lifecycleStatus: string; outcome: string; timestamps: { completedAt: string | null }; memories: Array<unknown>; artifactSummary: Array<unknown> };
type HistoryDto = { items: HistoryItem[]; invitations: Array<{ id: string; chronicleTitle: string; lifecycleStatus: string }>; nextCursor: string | null };
export function HistoryExplorer() {
  const [search, setSearch] = useState(""); const [query, setQuery] = useState(""); const resource = useResource<HistoryDto>(`/api/passport/history${query ? `?search=${encodeURIComponent(query)}` : ""}`);
  if (resource.state.status === "loading") return <LoadingState label="Reading Chronicle History" />; if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />;
  return <div className="harbor-stack"><form className="harbor-search" role="search" onSubmit={(event) => { event.preventDefault(); setQuery(search.trim()); }}><label htmlFor="history-search">Search your history</label><div><input id="history-search" value={search} onChange={(event) => setSearch(event.target.value)} maxLength={80} /><button className="button">Search</button></div></form>{resource.state.value.items.length ? <ol className="history-list">{resource.state.value.items.map((item) => <li key={item.id}><article><div><span className="harbor-badge">{item.lifecycleStatus}</span><h2>{item.chronicle.title}</h2><p>{item.timestamps.completedAt ? `Completed ${new Date(item.timestamps.completedAt).toLocaleDateString()}` : "Completion time unavailable"}</p><p>Version {item.chronicle.versionChecksum.slice(0, 12)}… · {item.memories.length} memories · {item.artifactSummary.length} artifact records</p></div><Link className="button" href={`/passport/history/${encodeURIComponent(item.id)}`}>Open record</Link></article></li>)}</ol> : <div className="harbor-empty harbor-empty--large"><h2>No Chronicle history found</h2><p>{query ? "No owner-authorized records match that search." : "Completed and terminal Voyage records will appear here without publishing their private details."}</p>{query && <button className="button" type="button" onClick={() => { setSearch(""); setQuery(""); }}>Clear search</button>}</div>}{resource.state.value.invitations.length > 0 && <section className="harbor-panel"><h2>Invitation history</h2><ul>{resource.state.value.invitations.map((item) => <li key={item.id}>{item.chronicleTitle} · {item.lifecycleStatus}</li>)}</ul></section>}</div>;
}

type HistoryDetailDto = HistoryItem & { participant: { name: string; role: string; crewRole: string | null }; timing: Record<string, { seconds?: number | null; accuracy?: string } | string>; completedChapters: Array<{ chapterId: string; title: string; completedAt: string }>; choiceSummary: Array<{ reason: string }>; reflection: { privateNote: string | null } | null; memories: Array<{ id: string; title: string; body: string | null; createdAt: string }>; keepsake: { status: string; generatedAt: string; participantCount: number } | null; provenance?: unknown };
export function HistoryDetail({ recordId }: { recordId: string }) {
  const resource = useResource<HistoryDetailDto>(`/api/passport/history/${encodeURIComponent(recordId)}`); const { csrfToken, setDirty } = usePersonalHarbor(); const [note, setNote] = useState(""); const [memory, setMemory] = useState({ title: "", body: "" }); const [message, setMessage] = useState(""); const [error, setError] = useState(false);
  useEffect(() => { if (resource.state.status === "ready") setNote(resource.state.value.reflection?.privateNote ?? ""); }, [resource.state]); useEffect(() => () => setDirty(false), [setDirty]);
  if (resource.state.status === "loading") return <LoadingState label="Opening Chronicle record" />; if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />; const value = resource.state.value;
  const mutate = async (url: string, method: string, body?: unknown) => { setMessage("Saving…"); setError(false); try { await responseBody(await fetch(url, { method, headers: { "content-type": "application/json", "x-csrf-token": csrfToken }, ...(body ? { body: JSON.stringify(body) } : {}) })); setMessage("Saved."); setDirty(false); resource.reload(); return true; } catch (cause) { setMessage(cause instanceof Error ? cause.message : "The change could not be saved."); setError(true); return false; } };
  return <div className="harbor-stack"><Link href="/passport/history">← Chronicle History</Link><section className="passport-record-hero"><span className="harbor-badge">{value.lifecycleStatus}</span><h2>{value.chronicle.title}</h2><p>{value.outcome} · version {value.chronicle.versionChecksum}</p><dl className="harbor-definition-list"><div><dt>Participant</dt><dd>{value.participant.name} · {value.participant.role}</dd></div><div><dt>Completed chapters</dt><dd>{value.completedChapters.length}</dd></div></dl></section><section className="harbor-panel"><h2>Version-pinned record</h2>{value.completedChapters.length ? <ol>{value.completedChapters.map((chapter) => <li key={chapter.chapterId}>{chapter.title} · {new Date(chapter.completedAt).toLocaleDateString()}</li>)}</ol> : <p className="harbor-empty">No exact completed-chapter events are available for this pinned version.</p>}{value.choiceSummary.map((summary, index) => <p className="harbor-field-hint" key={index}>{summary.reason}</p>)}</section><form className="harbor-panel harbor-form" onSubmit={(event) => { event.preventDefault(); void mutate(`/api/passport/history/${encodeURIComponent(recordId)}`, "PATCH", { privateNote: note || null }); }}><h2>Private reflection</h2><label>Note<textarea rows={6} maxLength={4000} value={note} onChange={(event) => { setNote(event.target.value); setDirty(true); }} /></label><button className="button button--primary">Save reflection</button></form><form className="harbor-panel harbor-form" onSubmit={async (event) => { event.preventDefault(); const ok = await mutate(`/api/passport/history/${encodeURIComponent(recordId)}/memories`, "POST", memory); if (ok) setMemory({ title: "", body: "" }); }}><h2>Add Chronicle Memory</h2><label>Title<input maxLength={120} required value={memory.title} onChange={(event) => { setMemory({ ...memory, title: event.target.value }); setDirty(true); }} /></label><label>Private memory<textarea rows={5} maxLength={4000} value={memory.body} onChange={(event) => { setMemory({ ...memory, body: event.target.value }); setDirty(true); }} /></label><button className="button button--primary">Save Memory</button></form><section className="harbor-panel"><h2>Private Keepsake</h2>{value.keepsake ? <><p>Status: {value.keepsake.status}. Generated {new Date(value.keepsake.generatedAt).toLocaleString()}.</p><div className="personal-harbor__actions"><button type="button" className="button" onClick={() => void mutate(`/api/passport/history/${encodeURIComponent(recordId)}/keepsake/consent`, "PUT", { scope: "DISPLAY_NAME", state: "GRANTED" })}>Allow my display name</button><button type="button" className="button button--quiet" onClick={() => void mutate(`/api/passport/history/${encodeURIComponent(recordId)}/keepsake/consent`, "PUT", { scope: "DISPLAY_NAME", state: "REVOKED" })}>Revoke name consent</button></div></> : <><p>A Keepsake is private until explicit participant consent permits scoped details.</p><button type="button" className="button" onClick={() => void mutate(`/api/passport/history/${encodeURIComponent(recordId)}/keepsake`, "POST")}>Prepare private Keepsake</button></>}</section><MutationMessage message={message} error={error} /></div>;
}

type MemoryDto = { id: string; recordId: string; chronicleTitle: string; title: string; body: string | null; referenceType: string | null; createdAt: string };
export function MemoriesExplorer() {
  const resource = useResource<{ items: MemoryDto[] }>("/api/passport/memories"); const { csrfToken } = usePersonalHarbor(); const [message, setMessage] = useState(""); const [error, setError] = useState(false);
  if (resource.state.status === "loading") return <LoadingState label="Reading Chronicle Memories" />; if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />;
  const remove = async (item: MemoryDto) => { if (!window.confirm(`Delete the private Memory “${item.title}”?`)) return; try { await responseBody(await fetch(`/api/passport/history/${encodeURIComponent(item.recordId)}/memories/${encodeURIComponent(item.id)}`, { method: "DELETE", headers: { "x-csrf-token": csrfToken } })); setMessage("Memory deleted."); setError(false); resource.reload(); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Memory could not be deleted."); setError(true); } };
  return <div className="harbor-stack">{resource.state.value.items.length ? <ol className="memory-grid">{resource.state.value.items.map((item) => <li key={item.id}><article className="harbor-panel"><p className="personal-harbor__eyebrow">{item.chronicleTitle}</p><h2>{item.title}</h2>{item.body && <p>{item.body}</p>}<p className="harbor-field-hint">Private · {new Date(item.createdAt).toLocaleDateString()}</p><div className="personal-harbor__actions"><Link className="button" href={`/passport/history/${encodeURIComponent(item.recordId)}`}>Open record</Link><button type="button" className="button button--danger" onClick={() => void remove(item)}>Delete</button></div></article></li>)}</ol> : <div className="harbor-empty harbor-empty--large"><h2>No private Memories yet</h2><p>Open a Chronicle History record to write a private Memory. Nothing here is published to your Profile.</p><Link className="button" href="/passport/history">Open Chronicle History</Link></div>}<MutationMessage message={message} error={error} /></div>;
}

type ArtifactItem = { id: string; name: string; type: string; state: string; status: string; chronicle: string; grantedAt: string | null; favorite: boolean; representation: string; archived: boolean };
type ArtifactListDto = { items: ArtifactItem[]; nextCursor: string | null; collections: Array<unknown>; assemblies: Array<unknown>; achievements: Array<unknown> };
export function ArtifactExplorer() {
  const [search, setSearch] = useState(""); const [query, setQuery] = useState(""); const resource = useResource<ArtifactListDto>(`/api/passport/artifacts${query ? `?search=${encodeURIComponent(query)}` : ""}`);
  if (resource.state.status === "loading") return <LoadingState label="Opening Artifact Cabinet" />; if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />;
  return <div className="harbor-stack"><form className="harbor-search" role="search" onSubmit={(event) => { event.preventDefault(); setQuery(search.trim()); }}><label htmlFor="artifact-search">Search artifacts</label><div><input id="artifact-search" value={search} onChange={(event) => setSearch(event.target.value)} maxLength={80} /><button className="button">Search</button></div></form>{resource.state.value.items.length ? <ol className="artifact-grid">{resource.state.value.items.map((item) => <li key={item.id}><article className="artifact-card"><div className="artifact-card__mark" aria-hidden="true">✦</div><span className="harbor-badge">{item.state}</span><h2>{item.name}</h2><p>{item.chronicle}</p><p>{item.status === "UNRESOLVED" ? "Witnessed in shared inventory; ownership remains unresolved." : "Backed by an authoritative grant record."}</p><Link className="button" href={`/passport/artifacts/${encodeURIComponent(item.id)}`}>View provenance</Link></article></li>)}</ol> : <div className="harbor-empty harbor-empty--large"><h2>No artifacts found</h2><p>{query ? "No owner-authorized artifacts match that search." : "Artifacts appear only after authoritative grant or explicit witnessed-state materialization."}</p></div>}</div>;
}

type ArtifactDetailDto = { id: string; artifact: { id: string; name: string; type: string; representation: string; accessibleRepresentation: string }; provenance: { chronicle: string; publishedVersionChecksum: string; sourceGrantEventId: string; recipientPolicy: string; state: string; custody: string; status: string; grantedAt: string | null; witnessedAt: string | null }; personalization: { favorite: boolean; privateNote: string | null; visibility: string; archived: boolean }; displayCases: Array<{ id: string; name: string }>; assemblies: Array<{ id: string; name: string; status: string }> };
export function ArtifactDetail({ artifactId }: { artifactId: string }) {
  const resource = useResource<ArtifactDetailDto>(`/api/passport/artifacts/${encodeURIComponent(artifactId)}`); const { csrfToken, setDirty } = usePersonalHarbor(); const [draft, setDraft] = useState<ArtifactDetailDto["personalization"] | null>(null); const [message, setMessage] = useState(""); const [error, setError] = useState(false);
  useEffect(() => { if (resource.state.status === "ready") setDraft(resource.state.value.personalization); }, [resource.state]); useEffect(() => () => setDirty(false), [setDirty]);
  if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />; if (resource.state.status === "loading" || !draft) return <LoadingState label="Opening artifact provenance" />; const value = resource.state.value;
  const save = async (event: FormEvent) => { event.preventDefault(); setMessage("Saving…"); setError(false); try { await responseBody(await fetch(`/api/passport/artifacts/${encodeURIComponent(artifactId)}`, { method: "PATCH", headers: { "content-type": "application/json", "x-csrf-token": csrfToken }, body: JSON.stringify({ favorite: draft.favorite, privateNote: draft.privateNote || null, visibility: draft.visibility, archive: draft.archived }) })); setDirty(false); setMessage("Artifact personalization saved."); resource.reload(); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Artifact could not be saved."); setError(true); } };
  return <div className="harbor-stack"><Link href="/passport/artifacts">← Artifact Cabinet</Link><section className="artifact-detail-hero"><div className="artifact-card__mark" aria-hidden="true">✦</div><span className="harbor-badge">{value.provenance.state}</span><h2>{value.artifact.name}</h2><p>{value.artifact.type} · {value.artifact.representation}</p><p>{value.artifact.accessibleRepresentation}</p></section><section className="harbor-panel"><h2>Provenance</h2><dl className="harbor-definition-list"><div><dt>Chronicle</dt><dd>{value.provenance.chronicle}</dd></div><div><dt>Record status</dt><dd>{value.provenance.status}</dd></div><div><dt>Recipient policy</dt><dd>{value.provenance.recipientPolicy}</dd></div><div><dt>Published version</dt><dd className="harbor-code-value">{value.provenance.publishedVersionChecksum}</dd></div><div><dt>Grant event</dt><dd className="harbor-code-value">{value.provenance.sourceGrantEventId}</dd></div></dl>{value.provenance.status === "UNRESOLVED" && <div className="harbor-callout"><strong>Witnessed, not claimed as owned.</strong><p>Shared inventory evidence is preserved without silently converting it into an ArtifactGrantReceipt.</p></div>}</section><form className="harbor-panel harbor-form" onSubmit={save}><h2>Private personalization</h2><label className="harbor-checkbox"><input type="checkbox" checked={draft.favorite} onChange={(event) => { setDraft({ ...draft, favorite: event.target.checked }); setDirty(true); }} />Mark as favorite</label><label>Private note<textarea rows={5} maxLength={2000} value={draft.privateNote ?? ""} onChange={(event) => { setDraft({ ...draft, privateNote: event.target.value }); setDirty(true); }} /></label><label>Visibility<select value={draft.visibility} onChange={(event) => { setDraft({ ...draft, visibility: event.target.value }); setDirty(true); }}><option value="ONLY_ME">Only me</option><option value="CREW_ONLY">Crew only</option><option value="REGISTERED_USERS">Registered members</option><option value="PUBLIC">Public</option><option value="UNLISTED">Unlisted</option></select></label><label className="harbor-checkbox"><input type="checkbox" checked={draft.archived} onChange={(event) => { setDraft({ ...draft, archived: event.target.checked }); setDirty(true); }} />Archive in my cabinet</label><button className="button button--primary">Save personalization</button><MutationMessage message={message} error={error} /></form></div>;
}

type SavedItem = { subjectType: string; subjectId: string; title: string; summary: string | null; href: string; savedAt: string };
export function SavedExplorer() {
  const resource = useResource<{ items: SavedItem[] }>("/api/passport/saved"); const { csrfToken } = usePersonalHarbor(); const [message, setMessage] = useState(""); const [error, setError] = useState(false);
  if (resource.state.status === "loading") return <LoadingState label="Reading saved Community items" />; if (resource.state.status === "error") return <ErrorState message={resource.state.message} retry={resource.reload} />;
  const unsave = async (item: SavedItem) => { setMessage("Removing saved item…"); setError(false); try { await responseBody(await fetch("/api/community/social/unsave", { method: "POST", headers: { "content-type": "application/json", "x-csrf-token": csrfToken }, body: JSON.stringify({ subjectType: item.subjectType, subjectId: item.subjectId }) })); setMessage("Saved item removed."); resource.reload(); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Saved item could not be removed."); setError(true); } };
  return <div className="harbor-stack"><section className="harbor-callout"><strong>Eligibility is checked each time this page opens.</strong><p>Private, unlisted, blocked, quarantined, archived, removed, or missing subjects are omitted without revealing why. Raw save rows are never returned.</p></section>{resource.state.value.items.length ? <ol className="saved-grid">{resource.state.value.items.map((item) => <li key={`${item.subjectType}:${item.subjectId}`}><article className="harbor-panel"><span className="harbor-badge">{item.subjectType.replaceAll("_", " ")}</span><h2>{item.title}</h2>{item.summary && <p>{item.summary}</p>}<p className="harbor-field-hint">Saved {new Date(item.savedAt).toLocaleDateString()}</p><div className="personal-harbor__actions"><Link className="button" href={item.href}>Open in Community</Link><button type="button" className="button button--quiet" onClick={() => void unsave(item)}>Remove saved item</button></div></article></li>)}</ol> : <div className="harbor-empty harbor-empty--large"><h2>No eligible saved items</h2><p>Save a public item in Community Harbor to return to it here.</p><Link className="button" href="/community">Explore Community Harbor</Link></div>}<MutationMessage message={message} error={error} /></div>;
}

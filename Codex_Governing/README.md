# Codex governance retrieval mirrors

The files in this directory are GitHub retrieval and backup mirrors only.

- Root `/AGENTS.md` is the canonical automatically discovered repository instruction file.
- Root `/PLANS.md` is the canonical planning standard that `/AGENTS.md` requires Codex to read at the applicable planning threshold.
- Root `/.codex/config.toml` is the canonical trusted project configuration.

Edit and validate the root files first, then synchronize `Agents.md` and `Plans.md` here. Do not treat these differently capitalized mirror files as active instructions or edit them as an independent source of truth.

---
title: Project Homeport Phase 5 Validation Record
audience: product-engineering
status: current
canonical_for: project-homeport-phase-5-validation-record
last_reviewed: 2026-08-03
---

# Project Homeport Phase 5 validation record

## Decision boundary

Project Homeport Phase 5 is implemented and exact-source browser validated on the named Homeport branch. This record preserves local branch evidence only; it does not convert synthetic validation into merge, deployment, production-provider, live-user, owner-acceptance, Phase 6, or Phase 7 proof.

| Identity                    | Value                                                  |
| --------------------------- | ------------------------------------------------------ |
| Phase 5 start               | `54372224fc9bf4b4fb42797ca58a5a224ffdb92a`             |
| Architecture freeze         | `bbe9659cc5077c834510c3e4db77aa362e45b6fd`             |
| Implementation anchor       | `39363729d6f98e66378f3173dbb72286cf6f62cf`             |
| Exact browser-tested source | `b9f1552b78857c36a45f25eb5fdfb7a7e09f102a`             |
| Branch                      | `codex/project-homeport-product-reality-recovery`      |
| Final publication           | Git handoff only; self-reference intentionally omitted |

## Exact-source evidence

- Production Next build/start plus Playwright: 6/6 serial groups passed in 4.6 minutes.
- Natural traversal: 42/42 ordinary routes reached through visible controls before direct-entry checks.
- Route receipts: 87 source-bound JSON receipts; no unexplained skip.
- Visual evidence: 29 checksum-verified PNG records at desktop, 390x844 mobile, and effective 200 percent zoom; all are `CODEX_VISUAL_REVIEW_ACCEPTED`.
- Dynamic coverage: source/detail, natural return, invalid ID, private denial, compact exit, and immersive exit.
- Token coverage: valid, invalid, expired, consumed, and revoked behavior without ordinary-navigation or committed-token leakage.
- Compatibility coverage: seven redirect aliases and five canonical context adapters with canonical targets proven first.
- Inventory finalizer: 85 nodes, 159 edges, 42 ordinary routes, 24 contextual routes, zero unexplained ordinary orphans; two final runs produced an identical Git diff hash.

## Required publication gates

The final publication requires the aggregate Homeport validators, documentation index/validation, Feature Catalog generation/validation, terminology, formatting, TypeScript, ESLint, privacy, SQLite and MySQL schema validation, ordinary client restoration, production build, and authoritative Sounding Line subsystem and mainline decisions. Their exact terminal decisions and digests belong in the final Git handoff so this source-bound record does not claim a future run or become self-referential.

## Rejected diagnostic attempts retained

Early focused runs exposed three genuine harness/product mismatches: a nonexistent keyboard control name, stale `AnimatePresence` route layers, and compatibility assertions that treated context adapters as redirects. Those attempts are not release proof. The corrected lane follows the visible Community link, replaces route layers atomically, tests Quartermaster's direct canonical exit, and performs a synthetic legacy invitation exchange into the mapped canonical session.

Visual review also rejected CSS `zoom: 2` as an inaccurate browser-zoom model because it preserved the desktop layout viewport. The accepted evidence uses a 720x500 CSS viewport at device scale 2, equivalent to a 1440x1000 physical viewport at 200 percent, and explicitly verifies the reflowed navigation controls.

The first prepublication mainline decision failed closed on `browser.access-sentinel` because its assertions still expected role-specific sign-in surfaces. The product correctly routed Quartermaster and Studio entry through the canonical `/sign-in` lifecycle with encoded return targets; the sentinel was updated to that governed contract, and its focused registered rerun passed all 3 cases with clean isolation and cleanup. After the active registry was regenerated, the next mainline decision failed only `retirement.matrix-proof` because two historical rows retained replacement IDs that no longer existed. Semantic reconciliation updated the retirement ledger, and the focused retirement proof then passed all 316 historical identities. Neither rejected decision is release proof; publication depends on later clean terminal subsystem and mainline decisions against the final change set.

---
title: Project Helm Amendment A3 Completion Receipt
audience: product-engineering
status: current
canonical_for: project-helm-amendment-a3-completion-receipt
last_reviewed: 2026-08-27
---

# Project Helm Amendment A3 completion receipt

## Completion declaration

`HELM_A3_COMPLETE=YES`

Amendment A3, **Ready the Room**, is complete on canonical main. It provides
the bounded Captain and ordinary Player pre-launch muster without reopening
accepted A1 or A2 work and without starting original Helm Phase 3.

`HELM_P3_STARTED=NO`

## Protected-main identity

| Item | Recorded value |
| --- | --- |
| Accepted product candidate | `ab5eb79b00d2d16710965a0d20039229cfd6aeda` |
| Candidate base | `e1a8d7eb1a6280bd3a6384070be519a72e0d9674` |
| Ordinary authority | [Sounding Line / Mainline Decision run 33090690178](https://github.com/Kgray44/treasurehuntSoT/actions/runs/33090690178), `PASS` |
| Protected pull request | [#489 — Project Helm A3: Ready the Room](https://github.com/Kgray44/treasurehuntSoT/pull/489) |
| Mainline merge commit | `488cee22e2a3c9e700409cd6d43872f11613b469` |
| Candidate and merge tree | `c70454ff892bd3222f3c888b08fea6c5147c8dc7` (identical) |

An earlier candidate, `8f58b0ec70b998b6b3f2af668bcf882a23cc9252`,
retains its failed authority receipt. Its full serial browser suite reached the
new seventh sign-in through one shared client boundary and correctly received
the product's six-attempt interactive-login limit. Commit `ab5eb79b` makes
each synthetic browser context use a distinct documentation-reserved test
network address, matching the existing per-client rate-limit boundary. The
full built-server suite and the exact replacement authority then passed.

## Evidence completed

- The accepted candidate's ordinary Sounding Line selected the affected Helm,
  component, safety, feature-catalog, production-build, and Chromium browser
  proof against the exact candidate tree.
- A clean local validation worktree passed the focused Helm and component
  suites (51 tests in 8 files), changed-source lint with no errors, and the
  complete six-journey built-server Chromium suite after the synthetic-client
  isolation repair.
- A fresh detached checkout at merge `488cee22` installed dependencies,
  generated the SQLite Prisma client, and completed `next build`.
- That landed checkout replayed the checked-in SQLite migration SQL into a
  task-owned database, seeded it, and passed the built-server Chromium A3
  journey (`1 passed`). The initial empty-database `prisma migrate deploy`
  attempt hit the repository's SQLite schema-engine opacity before tables were
  created; the SQL replay path is the repository's Sounding Line bootstrap and
  produced the passing landed database.

## Delivered boundary

The accepted source makes Captain-only, Captain + Player, and ordinary Player
waiting rooms distinct; shows status, presence, invitations, readiness,
reconnect guidance, Captain markers, and scoped existing lifecycle/succession
actions; distinguishes leaving a room from leaving a Voyage; and supports live
reconciliation, reduced motion, responsive layouts, and accessible controls.
It introduces no new lifecycle authority, schema migration, avatar system,
original Phase 3 command redesign, or deployment claim.

## Truth boundary

The recorded browser evidence uses only task-owned SQLite databases, synthetic
accounts, synthetic email outboxes, and production `next start` servers. It
does not claim deployment, production MySQL execution, live-provider delivery,
physical-device validation, or owner acceptance.


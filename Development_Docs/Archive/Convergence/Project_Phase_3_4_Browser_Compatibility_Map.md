# Phase 3-4 Browser Compatibility Map

## Failure inventory

Evidence was read from `validation-incident-evidence-20260726T130000Z-browser-failures`. The prior run used a marker-owned runtime and was stopped only after its owned process tree was identified; the eighth directory had begun but did not contain a completed artifact because of that controlled stop.

| Test file                    | Test title / project                                                                                                   | Old surface or assertion                                                                                | Observed canonical surface                                                                                                                                                                                          | Classification and required migration                                                                                                                                                                                                          |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `acceptance.spec.ts`         | `canonical Chronicle invitation journey keeps Player and Captain boundaries intact` / Chromium                         | Expected a legacy `chronicle_player` cookie after acceptance.                                           | Invitation acceptance creates the canonical account-rooted `wayfarer_account` session and deliberately deletes the legacy cookie.                                                                                   | Test defect. Assert the canonical session cookie is present and `chronicle_player` and `forever_gm` are absent; retain the Captain/Player boundary checks.                                                                                     |
| `acceptance.spec.ts`         | `canonical Chronicle invitation journey keeps Player and Captain boundaries intact` / Chromium                         | Created and launched the fixture through `BrowserContext.request`, then raced the Player loading shell. | The Captain signs in through the visible Console; same-origin page fetches use that browser session for setup, and the Player waits for the committed automatic root-to-Journal handoff before opening the Journal. | Test defect. This preserves the fixture boundary while proving browser-owned authentication, CSRF authorization, durable launch state, the Player handoff, and the semantic Journal-ready state.                                               |
| `animations.spec.ts`         | `first arrival supports skip, replay, reduced motion, and a semantic destination` / Chromium                           | Heading `Choose your place in the Tale`; buttons `Skip arrival` and `Replay gateway`.                   | Voyagewright role gateway heading is `Choose your role in Voyagewright`; its semantic controls are `Replay presentation` and the motion setting.                                                                    | Test defect. Verify the current role gateway, accessible motion control, reduced-motion fallback, replay, and Axe result without asserting retired animation timing.                                                                           |
| `chronicle-platform.spec.ts` | `Captain invitation, immutable version, Player runtime, archive, and revocation form one persisted journey` / Chromium | Expected `chronicle_player` after acceptance.                                                           | Canonical account-rooted Player identity uses `wayfarer_account`; the Player route remains the same.                                                                                                                | Test defect. Replace the cookie assertion while retaining the invitation, isolation, archive, revocation, CSRF, and no-leak assertions.                                                                                                        |
| `chronicle-studio.spec.ts`   | `Studio editor exposes searchable authoring tools and responsive isolated preview` / Chromium                          | Tabs `Blocks`, `Chapters`, and searchbox `Search story blocks`; old preview labels and CSS selectors.   | Studio uses `Passages`, `Chapters`, `Outline`, `Search Passages`, `Preview Passage`, and semantic editor controls.                                                                                                  | Test defect. Use roles/labels for canonical Studio navigation and preview, retaining keyboard, mobile, reduced-motion, immutable-version, and restore intent.                                                                                  |
| `chronicle-studio.spec.ts`   | `creator authors, aligns, publishes, plays, and reviews a media-rich tale` / Chromium                                  | `Create and open editor`.                                                                               | `Create and open Chronicle`.                                                                                                                                                                                        | Test defect. Replace the accessible-name locator only; preserve the complete Creator-to-published-player-review journey.                                                                                                                       |
| `command-center.spec.ts`     | `preview is nonmutating, stale commands conflict, and idempotency replays safely` / Chromium                           | `/quartermaster`; labels `Captain's name` and `Passphrase`; `Enter the chart room`.                     | `/quartermaster` redirects to `/captain/sign-in`; canonical labels are `Username`, `Password`, and `Enter Captain's Console`.                                                                                       | Test defect plus obsolete ownership. Replace retired Quartermaster UI setup with the Captain's Console sign-in and preserve the API-level nonmutation/idempotency assertions.                                                                  |
| `command-center.spec.ts`     | `targeted commands preserve side-quest order and audit staged work` / Chromium                                         | Expected populated legacy Quartermaster side-quest, map, artifact, and staged-action projections.       | The Captain's Console compatibility envelope deliberately returns those retired projections empty; the canonical seam is a prepared Chronicle command, its committed session event, sequence, and platform audit.   | Test defect. Replace the retired projection crawl with a browser-authenticated Captain preparation lifecycle: nonmutating preview, committed prepared event, canonical audit action, and idempotent replay without another sequence increment. |
| `command-center.spec.ts`     | `workspace routes preserve the newest Quartermaster surface and remain accessible` / Chromium                          | Retired `/quartermaster/*` workspace hierarchy and `.gm-grid`.                                          | Current Captain's Console owns `/captain`, `/captain/library`, invitations, voyages, and Studio handoff.                                                                                                            | Test defect. Replace the retired workspace crawl with Captain's Console navigation, invite/Crew controls, accessible responsive layout, and Axe coverage. The original run stopped before a complete error artifact was written.               |

No harness defect was found. No product defect was established by these artifacts: all observed responses followed the canonical role and session architecture. In particular, the absent `chronicle_player` cookie is intentional because invitation acceptance now writes `wayfarer_account` and deletes the legacy cookie.

## Canonical compatibility map

| Obsolete assumption                        | Canonical behavior and stable browser seam                                                                                                                                                                                                                                                                         |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Generic Tale role gateway                  | Voyagewright role gateway at `/`; heading `Choose your role in Voyagewright`, role cards, `Replay presentation`, and the accessible motion-setting button.                                                                                                                                                         |
| Quartermaster sign-in and workspace routes | Captain's Console at `/captain/sign-in` then `/captain`/`/captain/library`; use `Username`, `Password`, `Enter Captain's Console`, headings, navigation landmarks, and role-specific action controls.                                                                                                              |
| Legacy Player session cookie               | `wayfarer_account` is the account-rooted Player identity. The legacy `chronicle_player` cookie must remain absent for a newly accepted invitation.                                                                                                                                                                 |
| Studio blocks                              | Voyagewright Studio passages at `/studio/tales/[id]`; `Passages`, `Chapters`, `Outline`, `Search Passages`, `Preview Passage`, `Validate Chronicle`, and `Publish Chronicle`.                                                                                                                                      |
| Player runtime loading shell               | Player Chronicle first lands at `/player/playthroughs/[id]`; after the authoritative launch state is reconciled, it automatically hands off to `/journal`. Activate the visible `Open the journal` control there, then use `Skip ceremony` to assert the readable journal state rather than animation timing.      |
| History and rewards                        | Passport at `/passport` with `Chronicle Passport` and `Artifact Cabinet`; assert private, receipt-authorized projections.                                                                                                                                                                                          |
| Private package operations                 | Sealed Hold routes and APIs remain owner-authorized and provider-backed; browser assertions use visible grants/delivery/withdrawal results while setup can create isolated fixtures.                                                                                                                               |
| Community discovery and social state       | Harborlight Community controls use public/unlisted/Crew/private projections, Creator profiles, collections, reviews, comments, reports, Guides, and Voyage Logs; use visible controls and deny private identifiers.                                                                                                |
| Retired Quartermaster target lists         | Captain's Console browser setup remains the authoritative session boundary. For compatibility commands, use browser-context `fetch` with `credentials: "same-origin"`, current CSRF token, persisted Chronicle event/sequence, and `CHRONICLE_COMMAND_PREPARED` audit state rather than empty legacy target lists. |

For every replacement, setup may use an isolated authenticated API only to create durable test fixtures. The browser performs the user-facing action and verifies the visible and persisted outcome. Assertions retain privacy, IDOR, keyboard, mobile, reduced-motion, accessibility, and lifecycle coverage.

## Targeted repair record

| Old assumption                                                                                                                                     | Canonical replacement                                                                                                                                                                                                   | Product code changed                                       | Why the replacement is equivalent or stronger                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draggable Studio blocks announce `sortable story block`.                                                                                           | Draggable Studio passages announce `sortable Passage`.                                                                                                                                                                  | No. The product already exposed the canonical description. | The repaired assertion retains actual drag-and-drop and keyboard reorder acceptance while checking the current accessible name.                                                   |
| The terminal Studio block is `Tale Complete`.                                                                                                      | The terminal block is visibly and accessibly `Voyage Complete`.                                                                                                                                                         | No. The block registry already uses the canonical term.    | The acceptance flow still reaches the terminal block and validates its committed authoring semantics.                                                                             |
| Captain command tests authenticate through retired Quartermaster controls and use `page.request`, which omits the browser session in this harness. | Tests enter Captain's Console through `Username`, `Password`, and `Enter Captain's Console`, then use page-context same-origin fetch with the browser-owned session and CSRF token.                                     | No.                                                        | This verifies real browser authentication, preserves stale-command and idempotency contracts, and makes no manual cookie injection.                                               |
| The command preparation test traverses legacy side-quest/map/artifact lists returned by the Quartermaster projection.                              | It proves canonical prepared-command preview, committed `chronicle.commandPrepared` event, `CHRONICLE_COMMAND_PREPARED` audit, and idempotent replay.                                                                   | No.                                                        | The retired lists are intentionally empty. The new flow checks the authoritative Captain action, durable state, and no-duplicate guarantee rather than adapter-only display data. |
| Launching a Player Chronicle immediately exposes the Journal route.                                                                                | The Player first lands at the canonical Chronicle root; once the authoritative launched state is reconciled, the product automatically hands off to `/journal`, where the Player visibly opens and settles the Journal. | No.                                                        | The repaired acceptance waits for the committed launch handoff, then proves the accessible Journal opening and semantic ready state without relying on a transient loading shell. |
| Captain invitation cards use `role="row"` with generic child containers.                                                                           | Every invitation row exposes four `role="cell"` children inside the Captain invitation table.                                                                                                                           | Yes.                                                       | This fixes the actual Axe critical `aria-required-children` violation while retaining row structure, actions, and motion transition coverage.                                     |

The fourth targeted repair passed in a fresh marker-owned non-Git runtime: `Validation_Runs\\validation-20260726T145433706Z-22c4d47aedc2`. Its complete BrowserOnly gate also passed formatting, lint (81 existing warnings and zero errors), strict TypeScript, language validation, and 154 files / 1,074 Vitest tests. The browser target passed in 11.2 seconds, then the owned directory was removed with the marker-gated cleanup receipt. The retired validation worktree was not accessed or modified.

## Retained 2026-07-28 matrix taxonomy

The retained marker-owned runtime `Validation_Runs\\validation-20260728T091208274Z-76990858291c` discovered 316 Playwright tests: 118 passed, 155 failed, 28 intentionally skipped, and 15 did not run. The HTML report, first Chromium trace, first screenshot, and durable stdout/stderr were recorded before governed cleanup with SHA-256 values `383cabff198e6b25944b961462e620736f66eb096c3744afa9bf472d4201cf23`, `663f47495b8b933bfdbfdb794a209b98d72497f26396f660e7a5676d31480065`, `e39fe4992284b1f9b9fad4c391ef206f685698ac7c4126923fe44948d85b1337`, `5d667592813abf4f6aa3701f4b8ce30279b5095be6f655895b269c901adf3f0c`, and `f688ea4fdc825ad1140d5811a20cf930e31e862fc728324cdcb88b6994e27065` respectively.

The dominant Phase 3 accessibility viewport family has its first failure in Chromium: `phase3-accessibility-viewports.spec.ts`, `2560x1440 chapter-release is readable, reachable, and sound-independent`. It waited for a retired active progression overlay after the canonical Journal setup had already clicked `Skip ceremony` and settled the readable Journal. The equivalent WebKit-mobile case has the same navigation/readiness signature. This is one shared setup/order compatibility root (140 failures: 70 Chromium and 70 WebKit-mobile; 139 downstream cases), not 140 independent product failures. The next focused repair must keep the Journal's keyboard, responsive, reduced-motion, readable-result, and Axe assertions while moving the event assertion to the canonical Player contract.

The remaining independent families are invitation handoff (3; superseded by the retained invitation repair), detached animation skip controls (2), missing Studio duplicate action (2), visual-checkpoint timing (2), Project One fixture setup (2), public language leakage (2), WebKit Captain focus (1), and Sealed Hold WebKit connection reset (1). The three `step id not found` messages in stderr are Playwright reporter fallout, not product failures.

# Project Sounding Line Phase 1 Resource and Legacy-Harness Map

**Status:** declarations only; current harness preserved.

The policy names Node/Vitest capacity, validation runtime, Prisma generation, SQLite/MySQL, application/restart ports, Chromium/WebKit, media/private/trace roots, production build output, scanner, and external provider. Mutable resources remain exclusive or per-run declarations only; no Phase 1 lease runtime exists.

`npm run validate`, `scripts/test-all.ps1`, `playwright.config.ts`, `vitest.config.ts`, Prisma schemas/migrations, and `package.json` are untouched. The existing global validation lock, local runtime mirror, canonical SQLite safeguards, process ownership, browser artifacts, build, and restart proof retain their authority. Every plan emits `nonAuthoritative: true` and `execution: "forbidden"`.

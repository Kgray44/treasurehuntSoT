# Community Harbor release manifest

Manifest schema 1 requires listing ID, semantic version, immutable source `PublishedTaleVersion` ID, public metadata, license snapshot, and ordered attribution. Canonical key-sorted JSON is SHA-256 checksummed. A release is immutable; deprecation/replacement changes delivery metadata only.
